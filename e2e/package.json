{
  "name": "pocketdex-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cp ../firestore.rules emu.rules && firebase emulators:exec --only auth,firestore --project pocketdex-ffb4d \"node site.test.mjs\""
  },
  "devDependencies": {
    "firebase-tools": "^15.0.0",
    "playwright": "^1.50.0"
  }
}
